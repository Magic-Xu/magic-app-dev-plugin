interface:
  display_name: "Magic · 合并主干"
  short_description: "将已验收改动经 PR 与适用门禁合入主干，核验同步并清理对应分支。"
  default_prompt: "使用 $github-pr-mainline-release，把我已验收的当前改动合并到主干，完成适用门禁、同步和精确清理。"
policy:
  allow_implicit_invocation: true
